CREATE TABLE members (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT);
CREATE TABLE tasks (id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT, assigned_to INTEGER, deadline TEXT, is_completed BOOLEAN DEFAULT FALSE);